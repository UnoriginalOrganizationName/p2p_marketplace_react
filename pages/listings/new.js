import ListingForm from '../../components/ListingForm'

const listing_create = (props) => {

    return(
        <div>
            <h1 className='page-title'>Create New Listing</h1>
            <ListingForm></ListingForm>
        </div>
    )

}

export default listing_create